/**
 * Data Transfer Objects — tách khỏi {@link com.re.cinema_manager.model.entity}.
 * <p>
 * Quy ước package:
 * <ul>
 *   <li>{@code auth} — đăng ký, đăng nhập</li>
 *   <li>{@code profile} — hồ sơ người dùng</li>
 *   <li>{@code customer} — trang chủ, phim sắp chiếu</li>
 *   <li>{@code movie} — quản lý phim (Admin)</li>
 *   <li>{@code showtime} — lịch chiếu (Admin)</li>
 *   <li>{@code booking} — đặt vé, lịch sử, API</li>
 *   <li>{@code admin} — báo cáo, quản lý tài khoản</li>
 *   <li>{@code staff} — quầy vé, dashboard nhân viên</li>
 * </ul>
 */
package com.re.cinema_manager.dto;
